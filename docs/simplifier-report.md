# Simplifier Report — vkusvillbot

## Мёртвый код и неиспользуемые элементы

### 1. `db.py` — неиспользуемая функция `_extract_protein_per_100g()`
**Строки:** ~885-900
**Проблема:** Функция определена, но нигде не вызывается. Дублирует логику из `_extract_nutrition_metrics()` (строки 1017-1039).
**Рекомендация:** Удалить.

### 2. `sgr_agent.py` — неиспользуемая функция `_merge_items()`
**Строки:** 439-457
**Проблема:** Не вызывается нигде в проекте. Была предназначена для merge local и MCP результатов.
**Рекомендация:** Удалить.

### 3. `scripts/build_vector_index.py` — неиспользуемая функция `_decode_embedding()`
**Строки:** 48-49
**Проблема:** Определена, но не вызывается. Строка 137 декодирует эмбеддинги через `np.frombuffer()` напрямую.
**Рекомендация:** Удалить.

### 4. `scripts/build_vector_index.py` — `_embedding_bytes()` вызывается один раз
**Строки:** 44-45
**Проблема:** Однострочная функция, используется только на строке 121.
**Рекомендация:** Инлайнить: `np.asarray(emb, dtype="float32").tobytes()`.

---

## Дублирование

### 5. `db.py` + `product_retriever.py` — 5 идентичных функций

| Функция | db.py | product_retriever.py |
|---------|-------|---------------------|
| `_normalize_category_tokens()` | 903-911 | 20-26 |
| `_match_categories()` | 930-940 | 29-43 |
| `_parse_filter_expr()` | 943-964 | 141-163 |
| `_compare()` | 967-978 | 166-177 |
| `_extract_nutrition_metrics()` | 1017-1039 | 46-65 |

**Проблема:** Код скопирован между двумя модулями. Различия минимальны (только в `_extract_nutrition_metrics` разная нормализация).
**Рекомендация:** Создать модуль `utils.py` и импортировать в обоих местах. Экономия ~80 строк.

### 6. `llm_client.py` + `embeddings_client.py` — `_encode_header_value()`
**Строки:** llm_client.py:70-75, embeddings_client.py:73-78
**Проблема:** Идентичная функция в двух файлах.
**Рекомендация:** Вынести в общий модуль (`http_utils.py` или `utils.py`).

### 7. `config.py` + `sgr_agent.py` — дублирование `SgrConfig`
**Строки:** config.py:49-55, sgr_agent.py:110-117
**Проблема:** Класс определён дважды — как Pydantic BaseModel и как dataclass.
**Рекомендация:** Использовать только версию из `config.py`, импортировать в `sgr_agent.py`.

### 8. `main.py` — дублирование reply-chain логики
**Строки:** `_topic_ctx()` строки 74-80 повторяют `_reply_root_message_id()` строки 120-128
**Проблема:** Логика обхода reply-chain написана дважды.
**Рекомендация:** Вызывать `_reply_root_message_id(message)` из `_topic_ctx()` вместо копипасты.

### 9. `manual_sgr.py` — копипаста инициализации из `main.py`
**Строки:** 14-54
**Проблема:** Вся логика инициализации (Settings, DB, MCP, embeddings, retriever, agent) скопирована из main.py.
**Рекомендация:** Создать `setup_helpers.py` с функцией `async def create_agent()` и использовать в обоих местах.

---

## Переусложнённые абстракции

*Явных переусложнений не найдено — классы обоснованы.*

---

## Функции для инлайнинга

### 10. `sgr_agent.py` — `_to_float()`
**Строки:** 460-466
**Проблема:** Тривиальная обёртка, вызывается в одном месте (строки 289-296).
**Рекомендация:** Инлайнить или заменить на `float(x) if x else None`.

### 11. `sgr_agent.py` — `_strip_internal_fields()`
**Строки:** 433-436
**Проблема:** Одна строка логики (`clean.pop("updated_at", None)`), вызывается 2 раза.
**Рекомендация:** Инлайнить: `{k: v for k, v in item.items() if k != "updated_at"}`.

### 12. `db.py` — `clip()`
**Строки:** 704-707
**Проблема:** Вложенная 3-строчная функция, вызывается 2 раза.
**Рекомендация:** Инлайнить `text[:max_len] if text else None`.

---

## Тесты — дублирование и упрощения

### 13. `test_vector_search.py` — дублирование инициализации БД
**Строки:** 19-43, 131-207
**Проблема:** `_create_products_schema()` вызывается в двух тестах с полным copy-paste.
**Рекомендация:** Вынести в `conftest.py` как `@pytest.fixture`.

### 14. `test_sgr_parser.py` — повторяющийся паттерн payload
**Проблема:** Паттерн `payload = {...} → json.dumps → parse_llm_output` повторяется во всех тестах.
**Рекомендация:** Создать хелпер `_parse_payload(action, **kwargs)`.

### 15. `test_message_threads.py` — дублирование assertion
**Строки:** 49-56
**Проблема:** Паттерн `[m["content"] for m in messages]` повторяется 3 раза.
**Рекомендация:** Создать хелпер `assert_messages_equal(messages, expected)`.

---

## Итого

| Категория | Находок | Потенциальная экономия |
|-----------|---------|----------------------|
| Мёртвый код | 4 | ~30 строк |
| Дублирование | 5 | ~150 строк |
| Инлайнинг | 3 | ~20 строк |
| Тесты | 3 | ~50 строк |
| **Всего** | **15** | **~250 строк** |
